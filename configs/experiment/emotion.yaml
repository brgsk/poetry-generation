# @package _global_

defaults:
  - override /mode: exp
  - override /trainer: default
  - override /model: empath
  - override /datamodule: emotion
  - override /callbacks: default
  - override /logger: mlflow

name: "emotion"

data_dir: ${data_dir}/emotions
